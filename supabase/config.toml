project_id = "raifwxclurrnmjovmyyd"

# Edge Functions do agente (backend que o Lovable deixou fora de escopo).
# Descoberta via Receita Federal é ETL local (scripts/import_receita.ts), não função.

[functions.discover_google_maps]
verify_jwt = false

# qualify_lead: DEPRECADO em favor do score nativo do CRM (trigger trg_score_leads
# / calcular_score_lead). Mantido deployado temporariamente; será removido.
[functions.qualify_lead]
verify_jwt = false